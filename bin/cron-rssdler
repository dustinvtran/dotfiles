#!/bin/bash
# Keeps the specified program always running (to be used in cronjob).

export DISPLAY=:0

program="rssdler"
command="rssdler -c /home/dvt/.config/rssdler/config.txt -d"

if [[ -z $(pgrep $program) ]]; then
    $command
fi
